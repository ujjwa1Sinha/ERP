package com.transport.erp.trip.domain;

public enum TripEventType {
    TRIP_CREATED,
    DRIVER_ASSIGNED,
    VEHICLE_ASSIGNED,
    TRIP_STARTED,
    HALT_STARTED,
    HALT_ENDED,
    BREAKDOWN,
    ACCIDENT,
    FUEL_STOP,
    DESTINATION_REACHED,
    TRIP_COMPLETED,
    TRIP_CANCELLED
}
