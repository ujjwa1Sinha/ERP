global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'transport-erp-backend'
    metrics_path: '/actuator/prometheus'
    basic_auth:
      username: 'prometheus'
      password: 'changeme'
    static_configs:
      - targets: ['backend:8081']
